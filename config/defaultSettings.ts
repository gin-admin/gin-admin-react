export interface DefaultSettings {
  /**
   * primary color of ant design
   */
  primaryColor: string;
  pwa: boolean;
}

export default {
  primaryColor: '#1890FF',
  pwa: false,
} as DefaultSettings;
